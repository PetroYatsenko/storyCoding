extends ../layout

block scripts
  script.
    var steps = !{steps}
    var nextPath = !{next_path}
    var nextButton = !{JSON.stringify(next_btn)}
    var storyType = 'lesson'
    var wsAlert = !{JSON.stringify(alert)}
  script(src='/js/nu.js')
  
block content
  .container
    .col-sm-4.text-center
      h3= strings.img1_t
      img#img1(src= strings.img1)
      h3= strings.img2_t
      img#img2(src= strings.img2)
    .col-sm-8
      h2.text-center= strings.title
        | &nbsp;
        small
          span.label.label-info.text-lowercase= strings.subject
      .raw
        .col-sm-12#text
          hr
          each val, index in story_items
            p.story_text(id='story_item_' + index) !{val}
          div.text-right  
            button.btn.btn-info.btn-lg(type='button' id= next_btn)
              |>>>
          hr  