extends ../layout

block scripts
  script.
    var nextPath = !{next_path}
    var nextButton = !{JSON.stringify(next_btn)}
    var t = !{JSON.stringify(d)}
  script(src='/js/test.js')
  
block content  
  .container-fluid
    .row#title
      h2.text-center= str[state].title
        | &nbsp; 
        span.label.label-info.text-lowercase#subtitle
    hr
    .row#quest
    hr  
    .row#btns
    hr
    .row#answ
    div.text-right  
      button.btn.btn-info.btn-sm.disabled(type='button' id= next_btn) >>>
      
